"""Provider implementations for market-data integrations.

This package owns the concrete clients/routes/models for external providers.
Legacy `src.external_apis` modules now re-export from here.
"""
